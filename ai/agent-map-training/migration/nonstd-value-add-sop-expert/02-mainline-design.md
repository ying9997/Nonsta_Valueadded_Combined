# 02 主链路设计

状态：待 7 问确认后填写

## 使用条件

只有当 `01-constitution-7-questions.md` 中 1.1-1.7 已由用户填写并确认后，才开始填写本文件。

当前不得在本文件中代写业务边界答案。

## 设计目标

基于用户确认后的 7 个边界条件，设计非标增值 SOP expert 的 V0 主链路。

V0 主链路应控制在 6 个节点以内，并必须包含 `need_human` 兜底分支。

## 候选主链路占位

状态：待草稿

```text
待 1.1-1.7 确认后生成
```

## 节点设计模板

| 节点 | 状态 | 职责 | 输入 | 输出 | 是否确定性 | 依据 |
|---|---|---|---|---|---|---|
| 待填写 | 待草稿 | 待填写 | 待填写 | 待填写 | 待填写 | 待填写 |

## 必须确认项

状态：待用户确认

- [ ] 节点数不超过 6 个
- [ ] 生成指引步骤的节点被标记为 LLM 节点
- [ ] 确定性处理和 LLM 非确定性分析边界清晰
- [ ] 包含 `need_human` 分支
- [ ] `need_human` 条件由用户认可
- [ ] 每个节点都有输入、输出和消费方
