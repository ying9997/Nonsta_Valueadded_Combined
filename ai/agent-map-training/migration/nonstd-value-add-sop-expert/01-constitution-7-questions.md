# 01 Constitution: 7 个边界问题

状态：待用户填写

## 填写说明

本文件用于定义非标增值 SOP expert 的 V0 边界。请由用户手写回答，AI 不代答 1.1-1.7。

填写时建议保留每题的「确定事实 / 不确定项 / 明确不做」三类信息。没有结论的地方可以写「待确认」，但不要让 AI 自动补全。

回答完成后，本文件将作为后续主链路设计、节点点亮和 V0 验收的最高约束。

---

## 1.1 对应哪个业务流程段？

状态：待用户填写

- 非标增值 SOP 在入库后 / 履约中 / 售后哪个阶段触发？
- 典型用户触发场景是什么？例如：客服对话 / 工单 / 运营后台。

用户回答：

```text
待填写
```

## 1.2 用户输入是什么？

状态：待用户填写

- 用户会提供哪些信息？例如：订单号 / 商品信息 / 客户诉求。
- 哪些是确定事实？
- 哪些是不确定、需要追问的信息？

用户回答：

```text
待填写
```

## 1.3 可处理候选范围是什么？

状态：待用户填写

- V0 范围内，哪些「非标场景」可以被 SOP 指引覆盖？
- 哪些场景明确不做，不进入 V0？

用户回答：

```text
待填写
```

## 1.4 需要哪些 intent / route / branch？

状态：待用户填写

- V0 先定哪 2-3 个核心 intent？
- 每个 intent 对应什么 route？
- 每个 route 下有哪些 branch？

用户回答：

```text
待填写
```

## 1.5 输出契约是什么？

状态：待用户填写

- SOP 指引的输出格式是什么？例如：步骤列表 / 判断结论加依据 / 表格。
- 输出给谁消费？例如：客服 / 用户 / 下游系统。
- 输出中哪些字段必须稳定存在？

用户回答：

```text
待填写
```

## 1.6 哪些必须转人工？

状态：待用户填写

- 哪些情况必须进入 `need_human`？
- 例如：SOP 冲突 / 客户特殊协议 / 金额超限 / 无匹配 SOP。
- `need_human` 分支的判断条件是什么？

用户回答：

```text
待填写
```

## 1.7 V0 最小可跑通什么？

状态：待用户填写

- 最小主链路节点序列是什么？建议 4-6 个节点。
- MVP 验收标准是什么？
- 什么结果可以算 V0「能用」？

用户回答：

```text
待填写
```

## 确认记录

状态：待用户确认

- [ ] 1.1 已确认
- [ ] 1.2 已确认
- [ ] 1.3 已确认
- [ ] 1.4 已确认
- [ ] 1.5 已确认
- [ ] 1.6 已确认
- [ ] 1.7 已确认
