# Superpowers 设计与实施记录

本目录保存已经过 Henry 确认、会影响长期代码或业务规则的设计与实施计划。

- `specs/`：记录目标、已验证事实、设计边界和验收标准，文件名格式为 `YYYY-MM-DD-<topic>-design.md`。
- `plans/`：记录对应设计的可执行步骤和验证命令，文件名格式为 `YYYY-MM-DD-<topic>.md`。
- 临时调查日志、测试输出和敏感数据不得放入本目录；临时产物统一放入 `tmp/`。
- 需求或实现发生稳定变化时同步更新对应文件；不再适用的文件需经 Henry 确认后再删除。

