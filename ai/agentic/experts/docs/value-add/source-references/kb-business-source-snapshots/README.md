# KB Business Source Snapshots

本目录预留业务 KB 快照副本，用于支撑异常解释、SOP、处理限制和业务建议。

当前外部业务快照暂未复制到 repo；已整理过的可消费结论位于：

- `../../inbound-exception-value-added-process/`
- `../../inbound-exceptions/`
- `../../vasc-products/`
- `../../value-added-service-items/`

后续补齐快照时，应只新增 repo 内相对路径引用，不引用 workspace 外部路径。
