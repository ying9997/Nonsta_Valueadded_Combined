/**
 * LLM node declaration: `llm-answer-from-records`.
 *
 * This is not an executable code node. `build-summary-evidence` supplies a
 * bounded, evidence-ID-addressable message package; `format-output` validates
 * the model's references before it renders any user-facing summary.
 */

export {};
