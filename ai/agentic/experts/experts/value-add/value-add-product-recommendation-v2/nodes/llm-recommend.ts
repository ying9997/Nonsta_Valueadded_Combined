/**
 * LLM 节点声明：`llm-recommend`（非可执行代码节点）。
 * Prompt：`prompts/main.md`
 *
 * inputs: query, customerIntent, recommendationInput, filteredRecommendation, intentGuideKb, inputContext
 * outputs: analysisResult
 */

export {};
