/**
 * LLM 节点声明：`llm-classify`（非可执行代码节点）。
 * Prompt：`prompts/classify.md`
 *
 * inputs: query, customerIntent, recommendationInput, flowContextKb, inputContext
 * outputs: classificationResult
 */

export {};
