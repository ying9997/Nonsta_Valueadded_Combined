# --- Secrets & local config ---
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
secrets.json

# --- OS ---
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# --- Claude Code ---
.claude/

# --- Editors & IDEs ---
.idea/
*.swp
*.swo
*~
.project
.classpath
.settings/

# VS Code / Cursor: ignore local user settings, keep shared workspace config if added later
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# --- Temp & cache ---
*.tmp
*.bak
*.log
.cache/
tmp/
temp/
.tmp/

# --- Local tooling (scripts may run outside repo; ignore if copied in for testing) ---
node_modules/
__pycache__/
*.py[cod]
.venv/
venv/
.python-version

# --- Local exports not yet normalized into source-references ---
exports/
output/
scratch/

# --- Local interface verification sandbox ---
interface-verification-temp/
