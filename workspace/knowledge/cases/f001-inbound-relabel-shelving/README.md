# F-001 案例书

阅读顺序：**先 `rules.md`（主线 + 8 条）→ 再按规则回看 `CASEBOOK.md` 对应组。**

| 文件 | 现在是什么 |
|---|---|
| `rules.md` | 现行主线。从 12 组差别抽出，每条指回组号 |
| `CASEBOOK.md` | 7 格对照。组末尾挂了规则号 |
| `index.json` | 183 张种子索引（扩出的 10 张在 `_runs/20260901_oms_eb_expand/`） |

本机引用单号；对客不引用。不要把本目录当评测金标，不要直接当 Expert 运行时 KB。
