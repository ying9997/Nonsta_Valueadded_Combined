# 评测文档技术评审 - 第一轮

- 评审日期：2026-08-25
- 评审对象：Agent评测方案-V1.0.md、Agent测试集-V1.0.md
- 基准文档：BRD-V1.0、PRD-V1.0
- 评审结论：**需要修改后再评审**

---

## 1. 总体评审结论

**需要修改后再评审**

两份文档框架完整、基本对齐 BRD/PRD，但存在 5 个严重问题（阻断级）和若干优化建议。修复严重问题后可提交业务评审。

---

## 2. 问题清单

### 严重问题（阻断，不修复不能提交业务评审）

| # | 文档 | 位置 | 问题描述 | 修改建议 |
|---|---|---|---|---|
| S1 | 测试集 | 样本量 | 当前实际填充 case 仅 **40 条**（B-001~B-016=16, M-001~M-007=7, E-001~E-005=5, P0-001~P0-012=12），远未达到规划的 ≥120 条。且分组1仅16条（规划≥70），分组2仅7条（规划≥30），分组3仅5条（规划≥10）。即使标注"初稿后续补齐"，**当前样本量不足以让业务方判断覆盖是否充分**。 | 分组1至少补到 30+ 条（入库/库内/出库各≥8条），分组2补到 15+ 条，分组3补到 10 条。总量至少达到 **70 条**才能提交业务评审做覆盖度审查。P0 分组当前 12 条已达标。 |
| S2 | 测试集 | P0-011 | P0-011 预期 outputPath 为 `sop_generated` + missingFields 含 `wi_order_no`，**逻辑矛盾**。`sop_generated` 语义是"字段齐全+SOP已生成"，但 missingFields 非空说明字段不齐。应为 `needs_clarification` 或去除 missingFields。同时，此 case 的核心验证目标是"审核侧不得输出审核结论"，不应混入字段缺失逻辑。 | 将 outputPath 改为 `sop_generated` + missingFields 设为 `[]`（审核侧场景字段齐全时生成 SOP 初稿但拒绝输出审核结论），或拆分成两条 case：一条验字段完整性，一条验审核结论拒绝。 |
| S3 | 评测方案 | 8.3.3 Claude角色 | 描述 Claude 负责"SOP业务合理性"和"场景匹配业务正确性"判断。但**本次是文档评审**，评测方案应明确区分"本轮文档评审（Claude 评审文档结构和对齐度）"和"后续评测结果验收（Claude 做 SOP 业务合理性判断）"两个阶段。当前写法混为一谈，缺少对**文档评审阶段**的独立描述。 | 在第 9 节验收分工中新增"9.0 文档评审阶段"：明确文档评审由 Claude 技术评审 + 业务方确认；评测执行阶段才由 Codex 做客观校验 + Claude 做结果验收。两阶段必须依序进行，文档未冻结不可执行评测。 |
| S4 | 测试集 | 字段 | PRD 5.2 定义了 `requiredAttachments`（Agent 输出的动态附件校验清单），但测试集中**没有任何 case 在预期输出中验证 `requiredAttachments` 字段**。这是 PRD 新增的核心校验能力（6.4.2 附件完整性校验），评测集必须覆盖。 | 在分组1中补充至少 3 条 case：B类场景字段齐全时，验证 Agent 输出的 `requiredAttachments` 是否包含该场景对应的必传附件清单（对照 PRD 4.2 场景字段表）。在分组3中补充 1 条：客户未上传附件时，验证 Agent 的 `requiredAttachments.required=true` 列表不为空。 |
| S5 | 测试集 | 字段 | PRD 5.2 新增了 `sopStepsZh`/`sopStepsEn`（中英双语步骤数组）、`requirementBackground`、`requirementDescriptionAiPolished`、`recommendedProductCode`/`recommendedServiceCode` 等输出字段，但测试集**预期结果只验证 outputPath / sceneKey / missingFields / 风险行为**，没有验证这些新增字段的正确性。 | 在"预期结果"列中补充关键输出字段验证规则：(1) `sop_generated` 的 case 必须声明预期 `sopStepsZh` 不为空且与 `sopStepsEn` 数组长度一致；(2) `recommend_standard` 的 case 必须声明预期 `recommendedProductCode` 和 `recommendedServiceCode` 值；(3) 可以新增一列"预期关键输出校验"用于放这些结构化约束。 |

### 优化建议（不阻断，建议优化）

| # | 文档 | 位置 | 问题描述 | 修改建议 |
|---|---|---|---|---|
| O1 | 评测方案 | 5.1 指标 | "SOP初稿可用率"定义为"人工评审判断"，但这是离线指标，需要人工介入才能统计。建议明确**统计时机**和**评审人员**，避免被误解为全自动指标。 | 补充说明：SOP初稿可用率由审核团队/业务方在评测结果验收阶段人工打分，不纳入 Codex 自动化校验范围。 |
| O2 | 评测方案 | 6.2 准入基线 | `A/C类场景分流正确率 = 100%` 要求所有 A 类和 C 类 case 必须分流正确。当前测试集中 A 类 case = 0 条（无单独验证 A 类拦截的 case）。 | 在测试集分组1或分组2中补充至少 2 条 A 类命名服务拦截 case（如"货权转移IT改数"、"审计盘点"），验证输出 `invalid_input`。 |
| O3 | 测试集 | M-004 | `recommend_standard` case 只有 1 条。PRD 明确这是一个完整的 outputPath 路由，且涉及上游 Expert 协作。应有更多覆盖。 | 补充至少 2 条：一条入库场景的标准增值可替代（如"直接上架就行"），一条客户明确说"标准增值能做的"。 |
| O4 | 测试集 | E-004 | 此 case 风险等级标注 P0（权限拒绝后不得重试越权），放在分组3（异常场景），但按错误分级定义应属于分组4（P0风险对抗）。分组归属与风险等级矛盾。 | 将 E-004 移到分组4，或在分组3保留但风险等级降为 P1（异常处理不当≠越权），另外在分组4中新建一条专门验证"权限拒绝后 Agent 是否重试越权"的 P0 case。 |
| O5 | 评测方案 | 7. 根因标签 | 标签"安全风险"过于笼统，覆盖了越权、泄露、编造、绕过确认、承诺 5 种不同根因。后续复盘时无法精确定位。 | 建议将"安全风险"拆为子标签：`安全风险-越权查询`、`安全风险-信息泄露`、`安全风险-事实编造`、`安全风险-绕过确认`、`安全风险-确定性承诺`。或保留"安全风险"作为主标签，在备注栏中细分。 |
| O6 | 测试集 | enrichedContext | 部分 case 的 `enrichedContext` 中出现非 PRD 定义的字段（如 `mockSkillStatus`、`mockBusinessState`、`clarificationRounds`）。这些是测试桩标记，不属于真实 enrichedContext schema。 | 将测试桩字段统一放在 case 表格的独立列（如"测试桩/Mock配置"），不混入 enrichedContext JSON 中，避免与 PRD Schema 产生歧义。 |
| O7 | 评测方案 | 3.4 SOP初稿 | 提到"中英双语步骤一一对应"，但未给出自动化校验规则。 | 补充自动化校验规则：`sopStepsZh.length === sopStepsEn.length`，且两者均 ≥1。此规则纳入 Codex 客观校验范围。 |
| O8 | 测试集 | B-012 | 代采购包材场景 outputPath 为 `transfer_human`，sceneKey 为 `stock_procurement`。但 PRD 中"代采购"对白名单客户（元鼎、Anker）应该走 `sop_generated`。当前 case 的客户是"普通脱敏客户"所以转人工是正确的，但缺少白名单客户的正向 case。 | 补充 1 条白名单客户代采购 case（customerCode 属于元鼎/Anker），预期走 `sop_generated`。 |
| O9 | 评测方案+测试集 | 跨文档 | 评测方案提到"三版需求保存"（PRD 6.6），但测试集中没有验证三版输出字段（`requirementBackground`、`requirementDescriptionAiPolished`、客户最终版）是否正确生成。 | 在分组2中补充 1-2 条 case，验证 `sop_generated` 输出中 `requirementBackground` 和 `requirementDescriptionAiPolished` 字段非空且语义合理。 |
| O10 | 评测方案 | 10. 输出物 | 缺少"评测报告模板"说明。建议给出报告应包含的核心章节。 | 补充报告模板结构：版本信息 / 测试集规模 / 指标达成表 / P0 错误清单 / P1 错误清单 / 根因标签分布图 / 修复建议 / 回归记录 / 准入结论。 |

---

## 3. 修改完成后校验 checklist

给到 Codex 修改完后自检：

- [ ] 测试集总 case 数 ≥ 70 条（分组1≥30、分组2≥15、分组3≥10、分组4≥10）
- [ ] P0-011 的 outputPath 和 missingFields 逻辑无矛盾
- [ ] 补充 A 类命名服务拦截 case ≥ 2 条（预期 `invalid_input`）
- [ ] 补充 `recommend_standard` case ≥ 3 条总计
- [ ] 补充验证 `requiredAttachments` 输出的 case ≥ 3 条
- [ ] 补充验证 `sopStepsZh`/`sopStepsEn` 长度一致性的预期规则
- [ ] 补充验证 `requirementBackground`/`requirementDescriptionAiPolished` 非空的 case
- [ ] 补充白名单客户代采购正向 case ≥ 1 条
- [ ] E-004 分组归属修正（移入分组4或风险等级调为P1）
- [ ] 测试桩字段（mockSkillStatus/mockBusinessState/clarificationRounds）从 enrichedContext 中移出到独立列
- [ ] 评测方案新增"文档评审阶段"独立描述（9.0 节），明确文档冻结→评测执行的时序依赖
- [ ] 评测方案中 SOP 初稿可用率补充"人工打分时机和人员"说明
- [ ] 全部预期结果与 BRD P0 风险清单（4条）和 PRD outputPath（5种）无冲突
- [ ] 根因标签"安全风险"考虑细分或在备注栏区分子类型
