# udesk-stratified-v0.1 — **已归档（勿作现行评测主源）**

- 归档日期：2026-08-31  
- 状态：**archived**  
- 权威入口见：`../../EVAL-AUTHORITY.md`

## 为何归档

1. **缺少「需求收束」切点**：叶子多停在过早轮次，业务无法审、也无法证明试用价值。  
2. **粗桶分层过重**：B1～B8 全量配额适合上线后铺开，不适合「先试用再迭代」。  
3. **金标路径颠倒**：从吵对话往 OMS 贴标，噪音大；现行改为 **OMS 场景概述 + SOP → 倒查 Udesk**（见 `../oms-scene-f001-v0.1/`）。

## 本目录文件（保留不删）

| 文件 | 说明 |
|------|------|
| `sessions.json` / `leaves.jsonl` / `leaves.manifest.json` | 历史分层原料，**不作现行金标** |
| 本 README | 归档声明 |

运行痕迹：`_runs/20260829_udesk_sample/`（同期）同样仅作历史。

**禁止**在新评测报告、验收、业务评审中引用本集为「当前评测集」。
