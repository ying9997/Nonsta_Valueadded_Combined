# 会话摘要 02 · `udesk-empty:2026-06-11:WI49846413`

| 字段 | 值 |
|------|-----|
| sessionKey | `udesk-empty:2026-06-11:WI49846413` |
| 对话ID | **空**（源 CSV 无 `对话ID`；用日期+WI 定位） |
| 对话开始 | 2026-06-11 14:30 |
| 客户消息数 | 12 |
| 锚点 VASC | `VASC000000297108`（OMS 倒查；对话中未必写出 VASC 号） |
| OMS 场景 | 【入库】尺重/标签辨识后换标上架 |
| 关联 WI | `WI49846413`（在途）；OMS sop 另有新上架单 `WI50413717`、SKU `M010…10682794`→`M010…12129907` |
| F-001 贴合度 | **高**（意图澄清 + 特批路径说明） |

## 背景

客户要对入库单 `WI49846413` 上的 `A-B-2-2-4` 换标，且强调 **库内原有同标货物不要动**。

## 诉求

- 这种「只换本票、库内同标保留」能不能走增值？  
- 特批非标在哪下？仓内上架单在哪下？表怎么填？

## 已确认（对话内客服已给出）

- 只能尝试 **特批非标增值**：到仓后把 `A-B-2-2-4=M010000000010682794` 换为其他 SKU。  
- **必须先有仓内上架新单**，才能提交特批增值。  
- 入口：入库全部订单 → 该 WI 右侧「创建增值单」。  
- 仓内上架权限需客服侧申请（约两小时）；配好后在「新增入库单」下新单（换标后 SKU+数量）；再指引特批报价。  
- 客户当前填表内容被客服否定（「不是你这么填的」），需等权限后再按正确方式填。

## 卡点

- 客户不会区分「创建增值」表单填法 vs 仓内上架单。  
- 权限未就绪，**本通未完成提交**——适合做「缺前置条件时先说明路径 / 拦住乱填」，不适合当 happy-path 已提交金标。  
- 无正式 `对话ID`，评测引用须用合成 `sessionKey`。

## 摘要人备注

与 OMS sop（先勿上架 WI49846413 → 换 SKU 并补贴包裹标 → 用新单 WI50413717）一致；切叶时建议卡在「客服说明必须先下仓内上架单」之后客户追问填法处。
