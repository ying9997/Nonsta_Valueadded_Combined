# F-001 节点级理想 I/O：VASC000000274815

> 草稿供用户修改。理想输出从群聊审核行为 + 场景卡反推，不是凭空编造。  
> 材料：`traces-l4/VASC000000274815.trace.md`、`l4-results.json[0]`、场景卡 `f001-inbound-label-identify.json`

## 业务摘要（一句话）

异常包裹内椅子坐垫 SKU 无法判定；本单只做**开箱复测尺寸并回传**，审核员确认后仓库执行测量；**换标上架留待客户拿到尺寸后再提下一张增值单**（群聊：钟晓环「复测以后等客户再次提交增值处理上架」；许晓妍已审核）。

---

## ② context-bind

### 输入
- customerIntent 原文:
  > 用来辨识异常货物SKU  
  > 1件异常包裹，拆开外箱，复测箱内实物产品椅子坐垫长度与宽度。  复测后回传资料，附件有坐垫参考图
- OMS 字段: warehouseCode=DEBR2（群聊）, customerCode=19227270（群聊）, eventNo=EB0126042128953771, businessOrderNo=WI48988923
- 附件状态: {操作说明附件: missing, 商品和标签的对应关系: missing, 标签文件: uploaded}

### 理想输出
- boundKeys: `[eventNo, businessOrderNo, VAS_ATTR_REL_NWEON, 标签文件]`
- allEventNos: `[EB0126042128953771]`
- allBusinessOrderNos: `[WI48988923]`
- 没绑上但应该有的: warehouseCode / customerCode（case_level 转换未带入，群聊有；不影响本单主路径）

### 当前实际 vs 理想
- 实际 boundKeys / EB / WI 与理想一致。
- 仓库/客户码在 pipeline 输入为 `?`，与群聊不一致 → 转换层缺口，非 bind 逻辑错误。

---

## ③ check-requirement

### 输入
- customerIntent: 同上（**不含**「【入库】尺重/标签辨识后换标上架」场景概述名）✓

### 理想输出
- 应该提取的动作: `["辨识", "复测", "回传"]`（群聊落地动作是复测+回传；「辨识」在背景里指 SKU 判定目的）
- OBJECT_RE 应命中: `"货物"` 或 `"包裹"` / `"坐垫"`（对象清晰）
- ACTION_RE 应命中: `"辨识"` 或 `"复测"`（当前命中「辨识」可接受；理想可同时识别「复测/回传」）
- PURPOSE_RE 应命中: 本单去向是「回传资料 / 等客户再提上架」，**不是**当场「上架」  
  - 理想：要么命中「回传/等待客户」，要么靠 context（已有 EB+WI）兜底 complete=true
- complete: **true**（审核员未因「缺动作」退回，而是确认测量标准后通过）
- 如果 false，该追问什么？ N/A；群聊实际追问的是**测量标准（弧度如何量）**，属操作细则，不是 L1「缺对象/动作」

### 当前实际 vs 理想的差异
- 实际：OBJECT=`货物`✓，ACTION=`辨识`✓，PURPOSE=未命中但 context 兜底 → complete=true  
- 差异：PURPOSE 正则未覆盖「回传资料/等客户再提交」；本单靠 WI/EB 兜底过关，合理但脆弱。  
- 注意：文本**没有**「换标/上架」动作词 —— 与 OMS 场景名「换标上架」不完全同构；L1 仍 complete 是对的（本阶段需求自洽）。

---

## ④ match-template

### 输入
- normalizedRequirement: `"用来辨识异常货物SKU 1件异常包裹，拆开外箱，复测箱内实物产品椅子坐垫长度与宽度。 复测后回传资料，附件有坐垫参考图"`
- contextFacts 关键: sceneCode=20250407004, eventNo=EB0126042128953771

### 理想输出
- 应该提取的信号: hasIdentify=true；hasRelabel=false；hasShelve=false；hasPhoto=false（正文无「拍照暂存」）；interceptHold=false  
  - 群聊定性：本单是「尺重复测辨识」前半段，**尚未进入换标上架**
- 理想候选排序:

| 场景 | 理想分数 | 理由 |
|------|---------|------|
| F-001 | ≥3～中等 | 有「辨识」+ 复测尺重语境；场景卡 strong 含「辨识」「尺重」；但缺「换标/上架」→ 不宜虚高到「完整换标上架」满分 |
| A | 0～低 | 无「包裹类异常 / 包裹条码正常 / 商品条码异常 / 换商品标签」 |
| B | 0 | 无「拍照暂存 / 客户确认后再处理」作为本单主动作（虽「等客户」但不是拍照暂存场景） |

- 理想 decision: **supported**（OMS+群聊均按入库尺重辨识路径审过；或若严格要求「换标上架」齐全 → 可标 **ambiguous/需人工确认本单是否仅复测子类型**）
- 理想 reason: 低置信 supported_single / 或 ambiguous_scope_partial（用户可拍板）

### 当前实际 vs 理想的差异
- 实际：F-001=**3**（仅 strong:辨识），A=0（identify_without_package_exception），B=0；decision=`supported_single_supported_scene`  
- 差异：分数偏低但决策仍 supported —— 与「有辨识、无换标词」一致。  
- 缺信号：正文无「尺重/换标/上架」字面；「复测」未进场景卡 strong 列表。  
- **场景边界风险**：OMS 标 F-001「换标上架」，群聊执行却是「只复测等再提」——理想标注应记 `scope=measure_only_pending_followup_vas`，避免用完整 F-001 换标 SOP 模板硬套。

---

## ⑤ check-completeness

### 输入
- matchResult.supported=true, sceneKey=`inbound_label_identify`
- attachmentStatus: {操作说明附件: missing, 对应关系: missing, 标签文件: uploaded}
- WI 已绑定

### 理想输出
- complete: **true**（数据驱动策略：必填仅 LF + WI；本单均有）
- 如果 false，缺什么？ N/A
- 群聊里审核员实际追问了什么附件？  
  - **未追问**「操作说明附件 / 商品和标签对应关系」三件套  
  - 追问的是：**测量方法**（弧度 vs 垂直尺）+ 参考图含义（开箱量内品，非外箱）  
  - 与 pipeline「仅校验 LF」**一致方向**；额外操作细则目前不在 completeness 槽位

### 当前实际 vs 理想
- 实际 complete=true, provided 2/2 → 与理想一致。

---

## ⑥ generate-text（SOP 生成）

### 输入
- 全 context + matched F-001

### 理想 SOP 应包含的步骤（从群聊反推）
1. 按异常单 EB0126042128953771 / 入库单 WI48988923 定位 **1 件**异常包裹  
2. **拆开外箱**，取出箱内**椅子坐垫**实物（非量外箱）  
3. 按客户参考图测量点，用**垂直尺**量长度与宽度（弧度无关，无需沿弧测量）  
4. 拍摄含刻度的测量照片 + 实物照片  
5. **回传**实测长宽与照片（给客服/客户确认 SKU）  
6. **暂挂/等待**：等客户确认后再提交后续换标上架增值；**本单不做换标、不上架**

### 理想 SOP 不应包含的内容
- 不应写「审核通过」类结论  
- 不应编造 SKU / 条码 / 未提供的实物条码表  
- 不应把「换标 + 扫描上架到 WI」写成**本单必做步骤**（群聊明确二次提交）  
- 不应量外箱尺寸冒充内品（救火群曾纠偏：客户要开箱量产品）

### 理想 Reflection 结果
- 应检出：若初稿含换标/上架 → 与「处理方式：暂时 / 仅复测回传」冲突（**合理**）  
- 应检出：回传路径是否写清  
- 不应误杀：终稿若已改为「仅复测+暂挂」，应可 reflectionPass=true

### 当前 SOP vs 理想的差异
- 终稿 SOP（l4-results / trace）：已收敛为复测回传 + 暂挂、明确「本次不换标不上架」→ **与群聊理想高度对齐**  
- Reflection（`reflectionPass=false`，issues 仍指控「补贴标签并上架」「实物条码」等）→ **像在评初稿或过时条款**，与终稿正文不完全一致；理想 Reflection 应对终稿重评或消除误报  
- regenerated=true：说明初稿曾越权，方向正确

---

## 标注备注（给后续 golden）

| 节点 | 建议 gold 标签 |
|------|----------------|
| L1 | complete=true |
| L2 | scene≈F-001（子类型：尺重复测回传）；或 ambiguous_scope |
| L3 | complete=true（LF+WI） |
| L4 | SOP=measure_return_hold；禁止本单换标上架 |
