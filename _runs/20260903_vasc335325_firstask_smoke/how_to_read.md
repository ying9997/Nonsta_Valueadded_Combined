# 怎么读 VASC000000335325 首问 dry-run

不是正式准确率，不写入 eval-v0.1。

## 对照侧（方案 A 金标）

- 官方名 F-001，正文漏液拍照清点 → 不应 `sop_generated`。
- 转人工 / 场景不唯一 / 明确不是换标 → 对题。
- 群聊真人首轮问的是：拍哪些、6 面、上架还是销毁、要不要新 WI。没有人要 `实物→新SKU`。
- 仓归属当前代码没有，不问仓不算当前实现失败。

## 当前代码结果

| 项 | 值 |
|---|---|
| outputPath | `transfer_human` |
| 停在 | `match-template` |
| 需求门 | 通过（未再问数量或范围） |
| decision | `unsupported`（不是 `ambiguous`） |
| F-001 | score 0，负向信号 `weigh_or_photo_without_relabel` |
| 分数最高候选 | `inbound_photo_hold`（弱信号 拍照/拍摄，未 unique） |
| 对外草稿 | 「需求已完整，模板库只自动支持 F-001，转人工」 |

## 怎么判

- 没出 SOP、也没把 F-001 unique support → 当前能力与「先挡场景错位」一致。
- 草稿是通用「非 F-001 转人工」，**没有**问到群聊里那些拍照范围 / 6 面 / 新 WI。这是观察项，不是本轮失败条件。
- 旧跑「请补充数量或范围」在这条首问样本上**没有复现**。
