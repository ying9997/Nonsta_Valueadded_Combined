# 第 1 轮探针记录（2026-08-31）

来源：用户控制台 `__PA_PROBE.printTable()` 原文。  
夹具：`_tmp/20260831_pageagent_oms_probe/fixtures/B_侧边栏真实体验版.html`（经 `http://127.0.0.1:4173/`）  
原始表：`round1.tsv`

## 人话结论

- **组1**：CDN、`window.PageAgent`、`pageController` 都过。
- **组2**：没有截图格式 `{type,targetId}`。官方通道是 `clickElement(编号)` / `inputText(编号)`。4 个真实目标全对；错误编号 99999 失败且不改已填内容。
- **组3 第一件事（S-0）**：不能把 CSS 选择器直接交给它。工具只有 `click_element_by_index`、`input_text` 等按编号的。
- **组3 第二件事（S-3）**：前端用选择器找到框再译成编号，4 步全对。路径 B 这样走是通的。
- **组4 N-1**：人话写清楚时，4 个目标也对。
- **组4 N-2**：「选择上架」点成了「直接上架」，并且**自己点了提交**（脚本没让它提交）。
- **组4 N-3**：「选择入库」点成了「入库-补贴包裹条码」，不是「入库其他服务需求」。
- **组4 N-4**：同一页再用 JSON 点编号 14，仍是「原单上架」。

本页没有 Ant Design、没有仓库下拉，不能外推到真实 OMS 下拉。
