# 第 3 轮：提交拉黑后人话（2026-08-31）

来源：用户控制台 TSV。同一夹具。  
表：`round3-submit-blacklist.tsv`  
对照：`round1.tsv` / `round2.tsv`（提交未拉黑）

## 拉黑有没有生效

有。`I-5` 成功。`updateTree` 前 30 条里是「上一步 / 保存草稿」，没有「提交」。

| | 未拉黑（1、2 轮） | 拉黑提交（本轮，人话跑了两遍） |
|--|-------------------|--------------------------------|
| N-1 写清楚 | 填对；有一次口头建议提交 | 填对；**是否提交=否**（两遍） |
| N-2「选择上架」 | 点成直接上架，并**自己提交成功** | 仍点成直接上架；**是否提交=否**（两遍） |
| N-3「选择入库」 | 入库-补贴包裹条码 | 仍是入库-补贴包裹条码；**是否提交=否** |
| JSON 四点/填 | 对 | 对 |

第二遍 N-2 它自己写：页面上有提交，但「没有可用的提交按钮」，并提到可以先点保存草稿 `[29]`。这次没交成，也没看到草稿保存成功的 toast。说明拉黑能挡住提交，但人话仍会找下一个能点的收尾按钮。

## 没变的

含糊人话照样点错卡片。拉黑只限制「能不能点提交」，不纠正「上架 / 入库」点到哪一张。
