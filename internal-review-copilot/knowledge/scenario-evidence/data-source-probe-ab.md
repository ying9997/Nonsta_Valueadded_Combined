# A/B 数据源探测（Step 0）

- 日期：2026-09-02
- 范围：Udesk CSV、飞书群聊 xlsx、现有 OMS 脚本/_runs
- 本文件只写结构与计数。**不含**真实客户名、邮箱、手机、地址、公司名、真实 VASC/EB/WI
- 脱敏映射只在 `_tmp/20260902_ab_datasource_probe/id_mapping.json`（非正式资产）

---

## 1. Udesk CSV

| 项 | 值 |
|----|----|
| 路径 | `workspace/data/raw/data_udesk_log_database_增值.csv` |
| 规模 | 806 行，全部有 `messages` |
| 对话结构 | `messages` 内按「人工客服消息 / 客户消息」分段；另有 `对话ID`、`date`、`对话客户消息数`、`客服消息数` |
| 含 PII | **是**。列：`客户`、`客户邮箱`、`客户id`、`IP地址`、`IP所在地`；正文含手机、邮箱、附件 URL、客服姓名 |

关键词命中（正文 + 主题 + 场景分类，未去重会话）：

| 侧 | 命中行 | 去重对话约 | 带 VASC | 带 EB | 强词是否出现 |
|----|--------|------------|---------|-------|----------------|
| A | 24 | 约 20 | 10 | 14 | **无**「包裹类异常」「包裹条码正常」。有「商品条码异常」(9)、「补贴商品标签」(8)、「换商品标签」(7) |
| B | 30 | 约 20 | 7 | 3 | **无**「指定商品拍照暂存」。几乎全是口语「拍照暂存」(28)。「暂存区」「拍照核实」各 1 |

能支撑哪一步：

- Step 1–2：可抽真实会话，切 L1（「那个单」）和 L2 近邻/对抗（库内拍照暂存、拍照+换标混合）。
- **不能**直接出 A/B gold：会话没有最终审核状态、没有人工 SOP 定稿、没有取消/驳回原因。
- 抽到的「拍照暂存」多数是**库内标准产品 / 退货**，不是入库 §2.7。

可关联字段：`对话ID`；正文里的 VASC / EB / WI。不要用客户邮箱或客户名做关联。

脱敏策略：订单号换成稳定假号（同一 trace 内一致）；抹手机/邮箱/公司名/客服名/附件 URL；正式产物只用脱敏后的客户意图句。

---

## 2. 飞书群聊 xlsx

| 项 | 值 |
|----|----|
| 路径 | `workspace/data/raw/飞书群聊_非标增值讨论_20260421-20260801.xlsx` |
| Sheet | `汇总`（97 行）、`讨论明细`（812 行） |
| 明细列 | 群名称、日期、讨论ID、发起人、增值产品/服务、客户/对象、仓库、关联单号、摘要、对话详情、参与人、飞书链接、关键词 |
| 含 PII | **是**。发起人、客户/对象、参与人、真实单号、公司名 |

关键词命中（讨论明细）：A 33 行，B 13 行。A 侧多见「换商品标签 / 补贴商品标签 / 商品标签上架」+ VASC + WI。B 侧仍偏口语「拍照暂存」。

能支撑哪一步：

- 补「客服/销售怎么追问、后来补了什么」（L1→L2 信息补齐、上架前拦截、必须关联异常单）。
- **不能**当 gold：群聊不是 OMS 终态；摘要里的催审 ≠ 审核通过。
- 本轮抽了 2 条脱敏线程：漏贴商品标签急上架；发错货要拦截再换商品标签。都作 **derived** 线索。

可关联字段：`关联单号`（VASC/WI/EB）、日期、仓库。不要用发起人/客户名。

---

## 3. OMS / scripts / _runs

| 资产 | 能力 | 对本轮 |
|------|------|--------|
| `scripts/oms/pull_f001_oms_facts.py` | `pageQuery` + `getVasList` + `getEventOrder`，**写死** `sceneOverviewCode=20250407004` | 只能拉 F-001。本轮**未改脚本、未新拉** |
| `scripts/oms/expand_f001_by_eb.py` | 按 EB 扩旁单 | 为 F-001 断号设计，不是 A/B 池 |
| `_runs/20260901_oms_facts/` | F-001 183 张：完成 147 / 取消 22 / 终止 13；有 SOP、`isAuditThrough`、`cancelReason` | **仅 F-001** 可逐条核验后出 gold |
| `_runs/20260901_oms_eb_expand/extras_details.json` | EB 扩出旁单里混到 6 张 OMS 场景名=A、已完成、`isAuditThrough=Y` | SOP 过短或未写 §2.5 关键句。**旁证，不是 A gold** |
| `_tmp/20260831_oms_f001_expand/udesk_vasc_oms_cache.json` | 会话反查带 A/B 场景名，`exactScene=false` | 无线上终态字段。**不是 gold** |
| 本轮 live OMS | 现有脚本不能按 A/B 场景名/码拉全状态池 | **未拉。见 `OMS_PULL_TODO.md`** |

A/B 当前是否有可用 historical gold 候选：

| 场景 | 结论 |
|------|------|
| A | **没有可入 gold 的候选。** extras 6 张有终态+短 SOP，但未按「包裹条码正常 / 商品条码异常」核验。 |
| B | **没有。** extras 本轮未抽出 B 场景码；Udesk/群聊无审核结论。 |

---

## 4. 16 条 09-02 待审核

路径：`_runs/20260902_internal_review_dryrun_after_requirement_source_fix/`（用户点名的 `..._after_match_template_v02` 是同批 16 条的后续 dry-run）。

**只能 shadow。** 无最终审核结论。1 条 F-001 边界、1 条直接上架、14 条拦截同文。

---

## 5. 脱敏与落点

| 内容 | 落点 |
|------|------|
| 真实号 ↔ 假号 | `_tmp/20260902_ab_datasource_probe/id_mapping.json` |
| 脱敏 trace 草稿 | `_tmp/20260902_ab_datasource_probe/deid_traces.json` |
| 正式评测输入 | 只用假号与改写后的意图句，见 `eval/datasets/eval-v0.1/` |
