# OMS 场景概述编码核验（步骤 0）

- 日期：2026-09-02
- 授权范围：只核码，**不拉全池**
- Live `pageQuery`：**已完成**（登录后，每码只取 5 条 + `total`）
- 原始返回：`_tmp/20260902_oms_scene_code_verify/live_probe.json`
- 本文件不含客户名、邮箱、手机

---

## 这是什么码

字段：`sceneOverviewCode`（增值审核**场景概述编码**）。  
和 `OW01V1602`（原子「入库其他服务需求」）、Copilot `sceneKey` 都不是一回事。

| 码 | 场景名 | 本次结论 | 列表 `total`（不滤状态） |
|----|--------|----------|-------------------------|
| `20250407004` | 【入库】尺重/标签辨识后换标上架 | **已核。** 5/5 码名成对；`total=183` 与 F-001 已有池一致 | 183 |
| `20250407008` | 【入库】包裹类异常换商品标签上架 | **已核。** 5/5 码名成对，未见同码配别的名 | 73 |
| `20250522001` | 【入库】指定商品拍照暂存 | **已核。** 先从种子单原子读出码，再按码试探 5/5 码名成对 | 98 |

拉全池必须用 **码**，不能用场景名。`where[sceneOverviewName]=…` 会被忽略，两次都返回全库 `total=112241`，前 5 条是无关草稿/已下单。

---

## 对照（F-001）

`where[sceneOverviewCode]=20250407004` → 返回名全是「【入库】尺重/标签辨识后换标上架」，`total=183`。

---

## A · `20250407008`

`where[sceneOverviewCode]=20250407008`：

- `total=73`，本次只看了 5 条
- 5 条的列表字段都是：码 `20250407008` + 名「【入库】包裹类异常换商品标签上架」
- 5 条终态：已完成 3 / 异常终止 2（`isAuditThrough` 在列表里都是 Y，和 F-001 一样，终态要以 `status` / 原因为准）

本地旁证与 live 一致：extras 6 张、8/29 旧 live 单，都是这对码名。

注意：核的是**场景概述**，不是原子。带这个码的单，原子服务名有时是「库内其他服务需求」。SOP 仍要按 §2.5 另核，**不能只凭码进 gold**。

---

## B · `20250522001`

按名过滤无效。改走：

1. 对 cache 里带 B 名的 `VASC000000244686` 做 `getVasList` → 原子 `OW01V1602` 上读到 `20250522001` + 「【入库】指定商品拍照暂存」
2. `where[sceneOverviewCode]=20250522001` → `total=98`，本次只看了 5 条
3. 5 条都是这对码名；本次 5 条列表状态都是已完成

编码习惯仍是创建日：B 是 `20250522`，不是 `20250407xxx`。之前禁止猜码是对的。

SOP 仍要按 §2.7 另核（指定对象 + 拍照 + 暂存/等客户确认），**不能只凭码进 gold**。

---

## 本次未做

- 未拉 A/B 全池（73 / 98 只是列表 `total`，没有逐张拉 `sop`）
- 未改 `candidates.jsonl`
- 未写 CASEBOOK
- 未改运行时代码
